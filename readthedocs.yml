version: 2

mkdocs:
  configuration: python/mkdocs.yml

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    pre_install:
      - pip install --upgrade pip>=25.1
    install:
      - pip install --group docs python/.
