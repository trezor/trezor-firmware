# Project-specific configuration

# Filter of the model/board defined features for this specific project
uses = [
    "backlight",
    "backup_ram",
    "ble",
    "boot_ucb",
    "button",
    "consumption_mask",
    "display",
    "display_mono",
    "display_rgb565",
    "display_rgba8888",
    "dma2d",
    "framebuffer",
    "haptic",
    "hash_processor",
    "hw_revision",
    "iwdg",
    "layout_bolt",
    "layout_caesar",
    "layout_delizia",
    "layout_eckhart",
    "lockable_bootloader",
    "mcu_attestation",
    "nfc",
    "nrf",
    "optiga",
    "power_manager",
    "pq_secure_boot",
    "pvd",
    "rgb_led",
    "rtc",
    "sbu",
    "sd_card",
    "sdram",
    "secret",
    "secmon_header",
    "secure_aes",
    "smp",
    "suspend",
    "tamper",
    "telemetry",
    "touch",
    "touch_wakeup",
    "tropic",
]

# ELF sections to be included in the final binary.
# The order of sections is important.
# ".manifest" is the firmware manifest region at the image start in the
# Merkle-tree layout (absent, thus a no-op, on legacy builds); in the tree layout
# prodtest is just code committed directly by the manifest. ".vendorheader" and
# ".header" are the legacy vendor + image headers (absent, thus no-ops, in the
# tree layout).
elf_sections = [".manifest", ".vendorheader", ".header", ".flash", ".data"]

secmon_body_sections   = [".secmon_header", ".flash", ".data"]
secmon_header_sections = [".vendorheader", ".header"]

# xtask build options mapped to the cargo features.
[build-options]
bootloader-devel = { true = ["bootloader_devel"] }
dbg-console      = { swo = ["dbg_console_swo"], system-view = ["dbg_console_system_view"] }
debug-link       = { true = ["debuglink", "ui_debug"] }
perf-overlay     = { true = ["ui_performance_overlay"] }
production       = { true = ["production"] }
